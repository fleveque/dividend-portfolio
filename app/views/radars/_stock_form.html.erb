<%= form_with(url: update_stock_radar_path(radar, stock_id: stock.id), local: true, method: :patch, class: "flex items-center gap-2") do |form| %>
  <div class="flex flex-col">
    <%= form.label :target_price, "Target Price", class: "text-sm font-semibold" %>
    <%= form.number_field :target_price, value: stock.target_price, step: 0.01, class: "p-2 border rounded-lg focus:ring-2 focus:ring-blue-500 focus:outline-none" %>
  </div>
  <%= form.submit "Update", class: "bg-blue-500 text-white px-4 py-2 rounded-lg hover:bg-blue-600 transition-colors" %>
<% end %>
